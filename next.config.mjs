/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    // Set to true in production to enforce type checking
    // Set to false during development for faster builds
    ignoreBuildErrors: true
  }
};

export default nextConfig; 